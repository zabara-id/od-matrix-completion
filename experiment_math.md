# Математическое описание эксперимента восстановления OD-матрицы по потокам

Ниже описан эксперимент, в котором по заданной «истинной» OD-матрице строятся равновесные потоки в **модели Бекмана (UE)**, затем часть компонент потока «скрывается» маской наблюдений, и по частично наблюдаемым потокам восстанавливается OD-матрица методом **зеркального спуска** (с энтропийной геометрией) с **L1-регуляризацией** и фиксированными маргиналиями.

---

## 1) Обозначения

- $n$ — число зон (в эксперименте каждая зона соответствует вершине графа).
- $m$ — число рёбер.
- $D\in\mathbb{R}_+^{n\times n}$ — OD-матрица спроса, $D_{ij}\ge 0$.
- Диагональ не используется: $D_{ii}=0$.
- Маргиналии (исходящие/входящие):
  $$
  L_i=\sum_{j=1}^n D_{ij},\qquad W_j=\sum_{i=1}^n D_{ij}.
  $$
- Вектор потоков на рёбрах: $f\in\mathbb{R}_+^{m}$.
- Маска наблюдений потоков: $M\in\{0,1\}^{m}$, $M_e=1$ означает, что поток на ребре $e$ наблюдается. Поэлементное умножение обозначим $\odot$.

Множество допустимых OD-матриц с фиксированными маргиналиями:
$$
\mathcal{D}(L,W)=\left\{D\in\mathbb{R}_+^{n\times n}:\;
D\mathbf 1=L,\; D^\top\mathbf 1=W,\; D_{ii}=0\right\}.
$$

---

## 2) Граф и функции стоимости на рёбрах (BPR)

Используется реальная сеть SiouxFalls: ориентированный граф $G=(V,E)$ на $|V|=n=24$ вершинах (зоны совпадают с вершинами), с числом рёбер $|E|=m$ из данных сети (в используемом наборе $m=76$).

Каждому ребру $e\in E$ сопоставляется **функция времени проезда** (BPR):
$$
t_e(x_e)=t^0_e\left(1+\alpha_e\left(\frac{x_e}{c_e}\right)^{\beta_e}\right),
$$
где
- $x_e$ — поток на ребре $e$,
- $c_e>0$ — пропускная способность,
- $t^0_e>0$ — свободное время,
- $\alpha_e=0.15$, $\beta_e=4$ (в эксперименте одинаковы для всех рёбер).

В текущем эксперименте $c_e$ и $t^0_e$ берутся из данных сети: $c_e$ — capacity, $t^0_e$ — free flow time.

Производная (для полноты):
$$
t_e'(x_e)=t^0_e\cdot \alpha_e\beta_e\cdot \frac{1}{c_e}\left(\frac{x_e}{c_e}\right)^{\beta_e-1}.
$$

---

## 3) Модель Бекмана: равновесные потоки как функция OD-матрицы

### 3.1) Формулировка (Beckmann potential)

Для фиксированного спроса $D$ потоки $f(D)$ определяются как решение задачи равновесного назначения (Wardrop UE), эквивалентной выпуклой задаче Бекмана по суммарному потоку:
$$
\min_{f\in\mathcal{F}(D)}\;\Phi(f)\;=\;\sum_{e\in E}\int_0^{f_e} t_e(s)\,ds,
$$
где $\mathcal{F}(D)$ — множество потоков, реализуемых некоторым разбиением спроса $D$ по путям (стандартные ограничения сохранения потоков/спроса).

Градиент потенциала по потокам имеет простой вид:
$$
\nabla_f \Phi(f)=t(f)\in\mathbb{R}^m,\qquad [t(f)]_e=t_e(f_e).
$$

### 3.2) Численное решение: метод Франка—Вульфа (FW) + all-or-nothing (AON)

Равновесие аппроксимируется итерациями FW:

1. Инициализация: $f^{(0)}=0$.
2. На итерации $k$:
   - вычисляются текущие времена $t^{(k)}=t\!\left(f^{(k)}\right)$;
   - решается линейная подзадача FW
     $$
     y^{(k)}\in\arg\min_{y\in\mathcal{F}(D)}\langle t^{(k)},y\rangle,
     $$
     что соответствует **AON-нагрузке**: для каждого origin $o$ весь спрос $D_{o\cdot}$ отправляется по кратчайшим путям (по весам $t^{(k)}$) к каждому destination $d$;
   - обновление:
     $$
     f^{(k+1)}=(1-\gamma_k)f^{(k)}+\gamma_k y^{(k)},\qquad \gamma_k=\frac{2}{k+2}.
     $$

Остановка основана на относительном разрыве (диагностике FW):
$$
\mathrm{rgap}\;=\;\frac{\big|\langle f^{(k)},t(f^{(k)})\rangle - \mathrm{SP}(t^{(k)},D)\big|}{\max(\langle f^{(k)},t(f^{(k)})\rangle,\,10^{-12})},
$$
где $\mathrm{SP}(t^{(k)},D)=\sum_{o,d} D_{od}\,\mathrm{dist}^{(k)}_o(d)$ — суммарная стоимость кратчайших путей при весах $t^{(k)}$.

Итоговая аппроксимация равновесного потока:
$$
f(D)\approx f^{(K)}.
$$

---

## 4) Генерация синтетических наблюдений потоков

Пусть задана «истинная» OD-матрица $D_\mathrm{true}$.

1) Генерируем «истинные» потоки:
$$
f_\mathrm{true}=f(D_\mathrm{true}).
$$

2) Выбираем долю наблюдаемых рёбер $p\in(0,1]$ и строим независимую маску:
$$
M_e\sim\mathrm{Bernoulli}(p).
$$
(Если случайно получилось $\sum_e M_e=0$, одну компоненту делают наблюдаемой.)

3) (Опционально) добавляем шум на наблюдаемых компонентах:
$$
\hat f_e = f_{\mathrm{true},e} + M_e\,\xi_e,\qquad
\xi_e\sim\mathcal{N}\!\left(0,\;\sigma^2\max(|f_{\mathrm{true},e}|,1)^2\right),
$$
где $\sigma\ge 0$ — уровень шума.

Далее в целевой функции используется только $M\odot(\cdot)$, поэтому значения $\hat f_e$ при $M_e=0$ несущественны.

---

## 5) Выбор референса и маргиналий

Помимо «истины» $D_\mathrm{true}$ вводится референсная матрица $D_\mathrm{ref}$, которая фиксирует допустимое множество через маргиналии, и используется в инициализации.

1) по ней фиксируются маргиналии $(L,W)$ в допустимом множестве $\mathcal{D}(L,W)$.

Типичная схема:

- (Опционально) зашумить $D_\mathrm{true}$ мультипликативно и обрезать отрицательные значения:
  $$
  D_\mathrm{ref}\leftarrow \max\!\bigl(D_\mathrm{true}\odot(1+\varepsilon),\,0\bigr),\qquad \varepsilon_{ij}\sim\mathcal{N}(0,\sigma_\mathrm{ref}^2).
  $$
- Обнулить диагональ: $(D_\mathrm{ref})_{ii}=0$.
- (Опционально) «сохранить истинные маргиналии»: проектировать $D_\mathrm{ref}$ на маргиналии $L_\mathrm{true},W_\mathrm{true}$ методом IPF.

Затем в задаче восстановления фиксируются
$$
L = D_\mathrm{ref}\mathbf 1,\qquad W = D_\mathrm{ref}^\top\mathbf 1.
$$

---

## 6) Внешняя задача восстановления OD-матрицы (целевая функция)

Ищется $D\in\mathcal{D}(L,W)$, минимизирующая расхождение модельных и наблюдаемых потоков на наблюдаемых рёбрах, плюс L1-регуляризация (в текущем бенчмарке — к «истине» $D_\mathrm{true}$):
$$
\min_{D\in\mathcal{D}(L,W)}\;
\underbrace{\frac12\left\|M\odot\big(f(D)-\hat f\big)\right\|_2^2}_{\text{data term}}
\;+\;
\lambda\underbrace{\left\|D-D_\mathrm{true}\right\|_{1,\mathrm{off}}}_{\text{regularizer}}.
$$

Здесь $\lambda\ge 0$ — вес регуляризации, а норма берётся по внедиагональным элементам (диагональ фиксирована нулём):
$$
\left\|D-D_\mathrm{true}\right\|_{1,\mathrm{off}}=\sum_{i\ne j}\left|D_{ij}-\left(D_\mathrm{true}\right)_{ij}\right|.
$$

---

## 7) Градиент data term: Якобиан отображения $D\mapsto f(D)$

Обозначим невязку на наблюдаемых рёбрах:
$$
r(D)=M\odot\big(f(D)-\hat f\big)\in\mathbb{R}^m.
$$

Тогда градиент data term выражается через Якобиан
$$
J(D)=\frac{\partial f(D)}{\partial\,\mathrm{vec}(D)}\in\mathbb{R}^{m\times n^2}:
\qquad
\nabla_D\frac12\|r(D)\|_2^2=\mathrm{unvec}\!\big(J(D)^\top r(D)\big).
$$

Поскольку точный $J(D)$ для «жёсткого» AON не является гладким (зависит от дискретного выбора кратчайших путей), в эксперименте используются **приближённые** варианты $J(D)^\top r$:

### 7.1) Режим hard: «прямолинейный» Якобиан FW/AON

При фиксированных весах $t^{(k)}$ AON-нагрузка линейна по $D$:
$$
y^{(k)}=A(t^{(k)})\,\mathrm{vec}(D),
$$
где матрица маршрутизации $A(t^{(k)})$ имеет элементы
$$
[A(t^{(k)})]_{e,(o,d)}=
\begin{cases}
1,& \text{если ребро $e$ лежит на выбранном кратчайшем пути $o\to d$ при весах }t^{(k)},\\
0,& \text{иначе.}
\end{cases}
$$

Если рассматривать веса $t^{(k)}$ как «замороженные» (straight-through приближение), то Якобиан FW-итераций удовлетворяет рекурсии
$$
G^{(0)}=0,\qquad
G^{(k+1)}=(1-\gamma_k)G^{(k)}+\gamma_k A(t^{(k)}),
$$
и затем берут
$$
J(D)\approx G^{(K)},\qquad
\nabla_D\frac12\|r\|^2\approx \mathrm{unvec}\!\big((G^{(K)})^\top r\big).
$$

### 7.2) Режим soft_grad: сглаженный градиент через soft AON (logit по near-shortest)

Чтобы стабилизировать $J^\top r$, вводится сглаженная версия AON:

- для фиксированного origin $o$ и весов $t$ находятся расстояния $\mathrm{dist}_o(\cdot)$;
- для каждой вершины $v$ рассматриваются **входящие** рёбра $u\to v$, которые «почти кратчайшие»:
  $$
  \mathrm{slack}(u\to v)=\max\bigl(0,\;\mathrm{dist}_o(u)+t_{u\to v}-\mathrm{dist}_o(v)\bigr)\le \delta(v),
  $$
  где порог
  $$
  \delta(v)=\delta_\mathrm{abs}+\delta_\mathrm{rel}\cdot \mathrm{dist}_o(v).
  $$
- среди кандидатов выбирается переход по логит-правилу:
  $$
  \mathbb{P}(u\to v)\propto \exp\bigl(-\theta\cdot \mathrm{slack}(u\to v)\bigr),
  $$
  где $\theta>0$ управляет «жёсткостью» выбора.

Это задаёт распределение по путям и, следовательно, **ожидаемые** потоки $y_\mathrm{soft}$, которые гладко зависят от $D$ (при фиксированном $t$). Тогда вместо $(G^{(K)})^\top r$ используется вектор-Якобиан произведение $J_\mathrm{soft}^\top r$, вычисляемое динамическим программированием по DAG-порядку вершин (в порядке роста расстояний).

### 7.3) Режим auto_soft

Комбинированный режим: сначала используется hard, и при стагнации по значению цели происходит переключение на soft_grad.

Итоговый градиент внешней задачи:
$$
\nabla_D \mathcal{L}(D)\;\approx\; \mathrm{unvec}(J^\top r)\;+\;\lambda\,S(D)
\quad(\text{вне диагонали}),
$$
где $S(D)$ — субградиент L1-терма:
$$
S(D)_{ij}\in \partial |D_{ij}-(D_\mathrm{true})_{ij}|=
\begin{cases}
1,& D_{ij}>(D_\mathrm{true})_{ij},\\
[-1,1],& D_{ij}=(D_\mathrm{true})_{ij},\\
-1,& D_{ij}<(D_\mathrm{true})_{ij},
\end{cases}
\qquad i\ne j.
$$

---

## 8) Зеркальный спуск в OD-пространстве

Оптимизация ведётся на множестве $\mathcal{D}(L,W)$, используя энтропийную геометрию (KL-проксимальность).

### 8.0) Начальное приближение (SVD-модификация + проекция)

Стартовая точка $D^{(0)}$ строится из референса $D_\mathrm{ref}$ через SVD и эвристическую модификацию сингулярных значений:
$$
D_\mathrm{ref}=U\,\mathrm{diag}(\sigma)\,V^\top,\qquad
\widetilde D = U\,\mathrm{diag}(\widetilde\sigma)\,V^\top,
$$
где $\widetilde\sigma$ получается из $\sigma$ следующим образом (текущая конфигурация для SiouxFalls, $n=24$):
$$
\widetilde\sigma_0 = 0,\qquad
\widetilde\sigma_i=
\begin{cases}
2\sigma_i,& i\in\{17,18,\dots,n-2\},\\
\sigma_i,& \text{иначе},
\end{cases}
$$
то есть зануляется главный сингулярный компонент и усиливается часть «хвоста» спектра.

Далее $\widetilde D$ приводится в допустимое множество:
$$
D^{(0)}=\Pi_{\mathcal{D}(L,W)}^{\mathrm{KL}}\!\bigl(\max(\widetilde D,0)\bigr),
$$
то есть выполняется KL-проекция на заданные маргиналии $(L,W)$ методом IPF с обнулением диагонали.

### 8.1) Энтропийный зеркальный шаг (мультипликативное обновление)

Пусть $G^{(k)}=\nabla_D\mathcal{L}(D^{(k)})$ — (приближённый) градиент на итерации $k$ и $\eta_k>0$ — шаг.
Зеркальное обновление с KL-проксимальностью эквивалентно поэлементному правилу
$$
\widetilde D^{(k+1)}_{ij}=D^{(k)}_{ij}\exp\bigl(-\eta_k\,G^{(k)}_{ij}\bigr),\qquad i\ne j,
$$
и $\widetilde D^{(k+1)}_{ii}=0$.

### 8.2) Проекция на маргиналии (IPF / iterative proportional fitting)

После мультипликативного шага выполняется проекция на $\mathcal{D}(L,W)$ путём попеременного масштабирования строк и столбцов (Bregman-проекция в KL-метрике):

Инициализация: $D\leftarrow \widetilde D$, $D_{ii}=0$.

Далее повторять $T$ раз:
1) масштабирование строк:
$$
D_{ij}\leftarrow D_{ij}\cdot \frac{L_i}{\sum_j D_{ij}},
$$
2) масштабирование столбцов:
$$
D_{ij}\leftarrow D_{ij}\cdot \frac{W_j}{\sum_i D_{ij}},
$$
и после каждого шага обнулять диагональ.

Результат принимается как $D^{(k+1)}$.

### 8.3) Выбор шага: бэктрекинг

Шаг $\eta_k$ выбирается бэктрекингом: стартуя с некоторого $\eta_0$, шаг уменьшают $\eta\leftarrow\beta\eta$ (где $\beta\in(0,1)$), пока не выполнится убывание цели
$$
\mathcal{L}(D_\mathrm{cand}) < \mathcal{L}(D^{(k)}) - \varepsilon_\mathrm{imp}.
$$

---

## 9) Гиперпараметры, используемые в эксперименте

Ниже — набор фиксированных чисел, которые определяют эксперимент (по смыслу):

- **Наблюдения потоков:** доля наблюдаемых рёбер $p$; сид генерации маски; уровень шума $\sigma$ и сид шума.
- **Референс и маргиналии:** уровень шума $\sigma_\mathrm{ref}$ (если используется); флаг «сохранить истинные маргиналии» (IPF-проекция $D_\mathrm{ref}$ на $(L_\mathrm{true},W_\mathrm{true})$).
- **Регуляризация:** вес $\lambda$ у L1-терма $\|D-D_\mathrm{true}\|_{1,\mathrm{off}}$ (в одном из запусков используется $\lambda=5\cdot 10^2$).
- **FW (Beckmann, hard):** максимум итераций $K=200$; критерий остановки $\mathrm{rgap}\le 10^{-3}$; шаг $\gamma_k=2/(k+2)$.
- **FW (soft_grad):** $K=200$, $\theta=10$, $\delta_\mathrm{rel}=0.02$, $\delta_\mathrm{abs}=10^{-3}$.
- **Зеркальный спуск:** число внешних итераций (используется до $250$); начальный шаг бэктрекинга $\eta_0=10^{-2}$; $\beta=0.5$; минимум $\eta_\min=10^{-12}$; максимум попыток на итерацию $50$; IPF-проекция $T=30$ внутренних итераций.
- **Инициализация:** SVD-модификация спектра $D_\mathrm{ref}$ (зануление $\sigma_0$ и масштабирование части хвоста) перед IPF-проекцией на маргиналии.

Пример набора значений: $p\in\{0.1,0.3,0.5,0.7,0.9\}$, $\sigma=0$, $\lambda=5\cdot 10^2$, $\eta_0=10^{-2}$, $\theta=10$, сид маски $123$, сид шума $123$.

### Сравнение с вундервафлей

[ 4540.71062933  8144.0831075   4553.2068654   6022.4028907
  8131.58687142 14280.18669076 10255.64588979 14205.35983138
 18286.90153568  5371.09304426 18263.2941885   8649.05149051
 16087.35320687  6034.89912677  8812.52634749 12405.9018368
 12175.45919904 15779.49412827 12581.87292984 12115.71815718
  6828.93706715  8370.31014755 15900.27100271  6970.34025896
 21834.2366757  21888.52755194 17697.2899729  23103.55314664
 11047.0340259   8112.37579042  5419.84342066 17513.7910268
  8338.03071364  9806.47395363 10317.9765131   8302.92080699
 12539.86750979 12667.05811503 11071.57482686  9806.80517916
  9129.90063234  8324.72146944 23230.35230352  9033.66455887
 19058.56669678 18326.88949112  8345.13700693 11065.10087323
 11612.19512195 15410.14754592  8105.32971996 11558.3860283
  9947.21469437 15839.23517013 15456.85034628 19127.55194219
 19097.71153267  9886.35953026  8741.82475158 19134.02589581
  8720.11442337  6358.8678109   7007.73863294  6242.63775971
  8525.77536886 10274.16440831 18218.33785005  7008.76242096
  8570.1897019   9619.42186089  8421.28876844  9556.30834086
  7848.0277025  11098.79554351 10113.52002409  7881.48148148]

[ 4512.08490128  8136.68882728  4536.68882728  5959.03928906
  8112.08490128 14029.88966967 10036.60528191 14052.96206684
 18029.88966967  5200.         18052.96206684  8792.90861544
 15783.93544201  5983.64321507  8795.55518251 12504.99351672
 12107.2599948  15834.45077159 12532.24400979 12069.93583775
  6886.7544291   8385.73313842 15804.36127212  6850.67545945
 21733.25998256 21817.60684302 17723.841318   23113.63053782
 11052.13077295  8100.          5300.         17604.01391743
  8370.07028832  9771.92746814  9988.92895873  8404.42879636
 12306.67557023 12393.35775509 11106.64224491  9817.74155953
  9030.17512727  8392.94868855 23190.37500696  9082.20985975
 19050.5836368  18383.70846818  8411.73844409 11079.56056485
 11646.70956579 15353.51494397  8100.         11627.11114719
  9926.05973381 15871.77492864 15426.54846013 19046.25494916
 19084.77372322  9906.4613152   8717.24144288 19056.61262238
  8731.8331107   6316.66727304  7000.          6241.61661408
  8610.8049153  10303.46328863 18378.29758338  7000.
  8586.79601559  9653.88025171  8386.72804746  9624.4604672
  7889.86114114 11093.32442977 10252.42152938  7854.22071554]
